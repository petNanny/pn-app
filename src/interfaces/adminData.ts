// FIXME: this is a example a file in the interfaces file.
export interface IAdmin {
  _id: string;
  email: string;
  password?: string;
  name: string;
  token: string;
  createdAt: string;
  updatedAt: string;
  isActive: boolean;
}
